import React from 'react';
import Details from '../components/Details';
import PaperContainer from '../../../design-system/containers/PaperContainer';

const ComicById: React.FC = () => {
  return (
    <PaperContainer>
      <Details />
    </PaperContainer>
  );
};

export default ComicById;
